@model DateTime?

<div class="jumbotron text-center">
    <div class="well">
        @*<div>
            There are no available challenges at the moment.
        </div>*@
        <div>
            Яничке, няма challenge-и. Ай стига спами, ве!
        </div>

        @if (Model != null)
        {
            @*<div>
                The next challenge will be available at <strong><span class="label label-warning">@Model</span></strong>.
            </div>*@
            <div>
                Следващия challenge ще започне в <strong><span class="label label-warning">@Model</span></strong>.
            </div>
        }
    </div>
</div>